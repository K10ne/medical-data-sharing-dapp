# 基于以太坊的去中心化隐私医疗数据共享 DApp 需求文档

版本：v1.1  
适用场景：毕业设计、开题报告、系统设计说明书、后续论文实现章节  
默认技术路线：Ethereum 测试网/本地链 + Solidity 智能合约 + React DApp + Node.js 后端索引服务 + IPFS 去中心化存储 + 客户端混合加密

## 0. 本次优化说明

v1.1 在 v1.0 的基础上重点补强以下内容：

1. 明确系统不是“把医疗数据全部上链”，而是“链上确权授权 + 链下密文存储 + 客户端加解密”。
2. 明确毕业设计的可实现边界，防止 DID、零知识证明、紧急访问、科研脱敏等高级功能同时展开导致项目失控。
3. 补充授权撤销、密钥丢失、IPFS 可用性、链上访问记录成本、CID 隐私泄露等关键风险。
4. 增加 MVP、增强版、答辩加分项三层实现范围，便于按时间推进。
5. 增加导师可能追问的问题和答辩口径，让需求文档更像一个可被评审的工程设计。
6. 将系统定位为“患者主权数据共享 DApp”，但承认医生资质审核、事件索引和通知服务存在辅助性中心化组件。

## 1. 待确认事项

以下内容会影响系统范围、实现难度和论文表达重点。若未特别说明，本文档后续按“默认方案”设计。

| 编号 | 需要确认的问题 | 默认方案 | 影响 |
| --- | --- | --- | --- |
| C1 | 是否使用真实医疗数据？ | 使用模拟数据或公开脱敏样例数据 | 避免隐私合规风险，适合毕业设计演示 |
| C2 | 是否必须部署到以太坊主网？ | 本地 Hardhat 链 + 当前可用应用测试网演示，优先选择 Sepolia；实施前再次确认官方测试网状态 | 降低成本，便于答辩展示 |
| C3 | 学校是否指定技术栈？ | React + TypeScript + Solidity + Hardhat + Node.js/NestJS + PostgreSQL | 若学校指定 Java/Spring，可替换后端但保持架构不变 |
| C4 | 隐私技术深度要求？ | 客户端 AES-GCM 加密 + ECIES/X25519 密钥封装 + 链上授权 + 审计日志 | 难度适中且可实现，可扩展 ZKP/DID |
| C5 | 是否需要代币激励？ | 不设计交易型代币，仅设计授权、审计和可选积分 | 避免金融属性和监管复杂度 |
| C6 | 是否需要医生/机构真实资质认证？ | 使用管理员模拟审核医生、医院、研究机构资质 | 满足毕业设计闭环 |
| C7 | 是否需要移动端 App？ | 只做响应式 Web DApp | 降低开发量，保留移动端适配 |
| C8 | 是否需要接入真实医院系统？ | 不接入，使用 FHIR 风格数据结构模拟电子病历 | 保持实用性但避免接口复杂度 |
| C9 | 是否要求零知识证明？ | 作为高级扩展模块：医生资质匿名证明或研究访问资格证明 | 可根据时间决定是否实现 |
| C10 | 是否允许中心化后端？ | 允许存在“非信任后端”，只负责索引、缓存、通知，不保存明文数据 | 兼顾可用性与去中心化设计 |

补充说明：截至本文档修订时，ethereum.org 的网络文档仍将 Sepolia 定位为应用和智能合约开发的默认测试网，Hoodi 更偏验证者和协议升级测试。毕业设计实施时，应以当时 ethereum.org 官方网络文档为准，避免使用已经停止维护的测试网。

## 2. 项目背景与问题定义

传统医疗数据共享存在以下问题：

1. 医疗数据分散在不同医院或平台，患者很难掌握完整数据控制权。
2. 数据共享依赖中心化平台，平台可能滥用、篡改或泄露数据。
3. 患者授权过程不透明，数据被谁访问、何时访问、用于什么目的难以追踪。
4. 医生、医院、科研机构之间存在数据协作需求，但隐私保护、授权撤销和审计机制不足。
5. 完全将医疗数据上链不现实，原因包括隐私风险、成本高、链上存储能力有限、数据无法真正删除。

本系统设计一个基于以太坊的去中心化隐私医疗数据共享 DApp。系统不将医疗明文数据上链，而是将加密后的医疗数据存储到 IPFS 等去中心化存储网络，将数据摘要、所有权、授权记录、访问审计等关键信息写入以太坊智能合约。患者作为数据所有者，可以上传、管理、授权、撤销和审计自己的医疗数据；医生或研究人员只能在获得授权后访问加密数据。

## 3. 设计目标

### 3.1 总体目标

构建一个可演示、可验证、功能闭环完整的隐私医疗数据共享 DApp，实现“患者控制数据、链上授权审计、链下加密存储、前端可视化操作”的完整系统。

### 3.2 具体目标

1. 数据隐私保护：医疗数据在客户端加密，后端、IPFS 节点和区块链均不接触明文。
2. 数据所有权明确：数据资产与患者钱包地址绑定，链上记录数据资产的摘要、版本和状态。
3. 授权过程可验证：医生或研究人员发起访问申请，患者审批后生成链上授权凭证。
4. 访问过程可追踪：访问申请、授权、撤销、数据读取记录形成完整审计链路。
5. 去中心化与实用性兼顾：智能合约负责可信状态，后端负责索引、通知、搜索和演示体验。
6. 系统复杂度达到毕业设计要求：包含智能合约、DApp 前端、后端服务、链下存储、加密机制和测试验证。

### 3.3 设计原则

1. 隐私优先：任何医疗明文数据默认不离开用户浏览器。
2. 最小上链：链上只保存必须可信、必须可审计、数据量较小的信息。
3. 可演示闭环：所有核心功能都能在本地链或测试网上完整跑通。
4. 可解释边界：对区块链不能解决的问题，例如已下载数据无法强制删除，要在设计中明确说明。
5. 复杂度可控：高级隐私能力作为增强项，不影响核心流程验收。

### 3.4 威胁模型假设

系统默认面对以下风险：

1. IPFS 节点、网关或后端服务可能被未授权人员访问。
2. 医生或研究人员可能在授权后截屏、下载或二次传播数据。
3. 区块链上的交易、地址、事件和时间戳都是公开可见的。
4. 用户浏览器可能清理本地存储，导致加密私钥丢失。
5. 后端数据库可能被攻击者读取或篡改。
6. 恶意用户可能伪装成医生、重复提交申请或批量枚举患者地址。

系统不解决以下问题，但需要在论文中说明边界：

1. 不阻止授权方在解密后通过系统外方式泄露数据。
2. 不保证 IPFS 上的密文永久可用，必须配合 pinning 或备份策略。
3. 不证明医生现实世界身份绝对真实，毕业设计阶段通过管理员审核模拟。
4. 不对真实医疗诊断结果负责，系统只处理数据共享流程。

## 4. 系统边界

### 4.1 系统包含

1. 钱包登录与用户身份注册。
2. 患者、医生、医疗机构管理员、研究人员、审计员等角色。
3. 医疗数据上传、加密、版本管理、分类管理。
4. 数据访问申请、患者授权、授权过期、授权撤销。
5. 数据下载与客户端解密查看。
6. 链上审计日志与前端可视化时间线。
7. 后端事件索引、通知、数据目录缓存。
8. 合约测试、前端主要流程测试和系统演示脚本。

### 4.2 系统不包含

1. 不接入真实医院生产系统。
2. 不存储真实身份证号、手机号、真实病历等敏感信息。
3. 不实现医疗诊断、处方开具等业务。
4. 不承诺链下数据已被访问者删除，因为任何已下载数据都无法通过区块链强制销毁。
5. 不设计可交易代币经济系统。

## 5. 用户角色与权限

| 角色 | 说明 | 核心权限 |
| --- | --- | --- |
| 患者 Patient | 医疗数据所有者 | 注册身份、上传数据、查看数据、审批访问申请、撤销授权、查看审计 |
| 医生 Doctor | 诊疗数据使用者 | 提交访问申请、查看已授权数据、填写访问目的、查看授权期限 |
| 研究人员 Researcher | 科研数据使用者 | 申请脱敏或限定字段数据访问、查看统计型或脱敏数据包 |
| 医疗机构管理员 Institution Admin | 医院或机构管理者 | 审核医生/研究人员身份、维护机构信息、协助紧急访问 |
| 审计员 Auditor | 合规或系统监管角色 | 查看链上授权和访问审计，不查看医疗明文 |
| 系统管理员 Contract Admin | 合约管理角色 | 初始化系统参数、暂停异常合约、管理角色注册策略 |

权限设计原则：

1. 患者拥有数据控制权，但系统管理员不拥有患者数据明文。
2. 医生和研究人员不能直接搜索患者敏感信息，只能通过患者授权或数据目录申请。
3. 审计员可以验证访问行为是否合规，但不能访问医疗数据内容。
4. 后端服务没有修改链上授权状态的权限，只能读取事件和提供辅助服务。

## 6. 核心业务流程

### 6.1 用户注册与身份认证流程

1. 用户连接钱包，例如 MetaMask。
2. 前端读取钱包地址，提示用户选择角色。
3. 用户填写基础资料：
   - 患者：昵称、年龄段、可选病历标签。
   - 医生：姓名、执业编号、所属机构、科室。
   - 研究人员：机构、研究方向、项目说明。
4. 前端生成用户加密密钥对：
   - 签名身份：以太坊钱包地址。
   - 加密身份：浏览器生成 X25519/ECIES 密钥对。
5. 用户将加密公钥和 DID 文档摘要提交到链上身份合约。
6. 医生、研究人员需要机构管理员审核后才可以正式申请数据。

### 6.2 患者上传医疗数据流程

1. 患者进入“我的医疗数据”页面。
2. 选择上传类型：
   - 检验报告。
   - 影像报告。
   - 诊断记录。
   - 用药记录。
   - 手术记录。
   - 自定义文件。
3. 前端对文件或结构化表单生成标准化元数据。
4. 前端生成随机对称密钥 DataKey。
5. 使用 AES-GCM 在浏览器本地加密医疗数据，得到密文 Ciphertext。
6. 使用患者自己的加密公钥封装 DataKey，得到 ownerEncryptedKey。
7. 将密文上传到 IPFS，获得 CID。
8. 计算密文哈希、元数据哈希和 CID 哈希。
9. 调用智能合约 registerDataAsset，链上写入：
   - 数据资产 ID。
   - 所有者地址。
   - CID 哈希或加密 CID。
   - 密文哈希。
   - 元数据哈希。
   - 数据类别。
   - 版本号。
   - 创建时间。
10. 后端监听 DataAssetRegistered 事件，建立数据目录索引。

设计要求：

1. 原始医疗明文不进入后端。
2. 链上不保存姓名、身份证号、病历明文、明文 CID。
3. 前端显示数据时，优先展示患者本地解密后的元数据。

### 6.3 医生申请访问流程

1. 医生进入“数据申请”页面。
2. 输入患者地址、患者分享码或扫描患者授权二维码。
3. 选择申请的数据范围：
   - 单个数据资产。
   - 某一类别数据，例如最近一次血检报告。
   - 某个时间范围内的数据。
4. 填写访问目的：
   - 诊疗。
   - 复诊。
   - 会诊。
   - 科研。
   - 保险或其他，不建议默认开放。
5. 设置期望访问期限，例如 24 小时、7 天、30 天。
6. 调用合约 createAccessRequest，生成链上申请。
7. 患者前端收到通知，进入“待处理申请”。
8. 患者查看医生资质、申请目的、数据范围、期限。
9. 患者审批后，前端解封装自己的 DataKey，再用医生公钥重新封装 DataKey。
10. 调用合约 approveRequest，写入授权范围、过期时间和加密密钥引用。
11. 医生获得授权后，从 IPFS 获取密文，在浏览器本地解密查看。

### 6.4 授权撤销流程

1. 患者进入“授权管理”页面。
2. 查看所有有效授权，包括访问者、数据范围、目的、过期时间、最近访问记录。
3. 选择撤销某个授权。
4. 调用合约 revokeGrant，将授权状态更新为 Revoked。
5. 后端监听事件并通知被撤销用户。
6. 前端在下次访问时检查链上授权状态，拒绝继续解密。

撤销限制说明：

1. 撤销只能阻止未来的系统内访问。
2. 对已经被授权方下载并离线保存的数据，区块链无法强制删除。
3. 系统通过水印、审计记录、访问目的约束、授权期限和版本密钥轮换降低滥用风险。

### 6.5 审计追踪流程

1. 系统对以下事件进行链上记录：
   - 用户注册。
   - 资质审核。
   - 数据资产注册。
   - 访问申请创建。
   - 患者审批。
   - 授权撤销。
   - 医生确认读取。
2. 前端“审计中心”以时间线展示：
   - 谁申请。
   - 申请什么数据。
   - 用于什么目的。
   - 是否通过。
   - 何时访问。
   - 授权是否过期或撤销。
3. 审计员可以按地址、资产 ID、机构、时间范围过滤审计记录。

### 6.6 紧急访问流程

紧急访问作为高级设计功能，可实现为毕业设计加分项。

1. 医生发起 emergencyAccessRequest，填写紧急原因。
2. 机构管理员进行二次确认。
3. 合约生成短时授权，例如 2 小时。
4. 所有紧急访问默认高亮写入审计日志。
5. 患者登录后可查看紧急访问记录并提交异议标记。

默认实现建议：

1. 不直接绕过患者密钥。
2. 仅演示“紧急申请 + 管理员确认 + 审计记录”流程。
3. 若没有患者在线审批，紧急访问只能访问患者预先设置的“急救摘要数据包”，例如血型、过敏史、慢性病史。

### 6.7 科研数据共享流程

1. 研究人员提交研究项目说明。
2. 机构管理员审核研究人员身份。
3. 研究人员发起对某类脱敏数据的访问申请。
4. 患者可选择加入某个研究项目，并授权特定字段。
5. 系统生成脱敏数据包：
   - 去除姓名、联系方式、身份证等直接标识符。
   - 对年龄做区间化处理。
   - 对时间做月份级或季度级处理。
   - 对稀有病种样本添加风险提示。
6. 研究人员只能访问授权范围内的脱敏数据。
7. 所有使用行为进入审计日志。

## 7. 功能性需求

### 7.1 身份与角色模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-IDENT-001 | 用户可以通过以太坊钱包连接系统 | 必须 |
| FR-IDENT-002 | 用户可以选择患者、医生、研究人员、管理员等角色注册 | 必须 |
| FR-IDENT-003 | 系统为每个用户维护链上身份记录和加密公钥 | 必须 |
| FR-IDENT-004 | 医生和研究人员注册后需经机构管理员审核 | 必须 |
| FR-IDENT-005 | 用户可以更新个人资料哈希和加密公钥 | 应该 |
| FR-IDENT-006 | 支持 DID 文档摘要，用于扩展去中心化身份 | 可选 |

### 7.2 医疗数据管理模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-DATA-001 | 患者可以上传医疗数据文件或结构化病历数据 | 必须 |
| FR-DATA-002 | 前端必须在本地完成数据加密 | 必须 |
| FR-DATA-003 | 系统将密文上传到 IPFS，并保存 CID 引用 | 必须 |
| FR-DATA-004 | 合约记录数据资产摘要、类别、版本、所有者和状态 | 必须 |
| FR-DATA-005 | 患者可以查看自己上传的数据列表 | 必须 |
| FR-DATA-006 | 患者可以为数据添加标签，例如血检、影像、用药 | 应该 |
| FR-DATA-007 | 支持数据版本管理，更新数据时产生新版本 | 应该 |
| FR-DATA-008 | 支持将旧版本标记为停用，但不删除链上记录 | 应该 |

### 7.3 访问申请与授权模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-AUTH-001 | 医生可以对患者数据发起访问申请 | 必须 |
| FR-AUTH-002 | 申请必须包含访问目的、数据范围、期限和申请人身份 | 必须 |
| FR-AUTH-003 | 患者可以批准或拒绝访问申请 | 必须 |
| FR-AUTH-004 | 授权成功后，合约生成授权记录 Grant | 必须 |
| FR-AUTH-005 | 授权记录必须包含过期时间、授权范围和状态 | 必须 |
| FR-AUTH-006 | 患者可以撤销尚未过期的授权 | 必须 |
| FR-AUTH-007 | 前端访问数据前必须检查链上授权状态 | 必须 |
| FR-AUTH-008 | 支持按数据类别或时间范围批量授权 | 应该 |
| FR-AUTH-009 | 支持紧急访问申请和短时授权 | 可选 |

### 7.4 数据访问与解密模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-ACCESS-001 | 被授权用户可以查看可访问的数据列表 | 必须 |
| FR-ACCESS-002 | 被授权用户可以从 IPFS 获取密文数据 | 必须 |
| FR-ACCESS-003 | 被授权用户只能在浏览器本地解密数据 | 必须 |
| FR-ACCESS-004 | 解密前必须验证密文哈希与链上记录一致 | 必须 |
| FR-ACCESS-005 | 数据展示页面应显示访问目的、授权期限和水印 | 应该 |
| FR-ACCESS-006 | 用户访问数据时应提交链上或后端访问确认记录 | 应该 |

### 7.5 审计模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-AUDIT-001 | 系统记录关键操作事件 | 必须 |
| FR-AUDIT-002 | 患者可以查看自己数据的访问申请和授权记录 | 必须 |
| FR-AUDIT-003 | 审计员可以按条件检索访问记录 | 应该 |
| FR-AUDIT-004 | 审计时间线应清晰展示事件顺序 | 应该 |
| FR-AUDIT-005 | 系统应支持导出审计报告摘要 | 可选 |

### 7.6 通知模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-NOTI-001 | 患者收到新的访问申请通知 | 应该 |
| FR-NOTI-002 | 医生收到授权通过、拒绝、撤销和过期通知 | 应该 |
| FR-NOTI-003 | 通知由后端根据链上事件生成 | 应该 |
| FR-NOTI-004 | 用户可以标记通知已读 | 可选 |

### 7.7 科研脱敏共享模块

| 需求编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-RESEARCH-001 | 研究人员可以创建研究访问申请 | 应该 |
| FR-RESEARCH-002 | 患者可以选择加入研究项目 | 应该 |
| FR-RESEARCH-003 | 系统提供基础脱敏规则 | 应该 |
| FR-RESEARCH-004 | 脱敏数据访问必须保留审计记录 | 应该 |
| FR-RESEARCH-005 | 可选支持零知识资质证明 | 可选 |

## 8. 非功能性需求

### 8.1 安全性

1. 医疗数据必须在客户端加密后再上传。
2. 后端和 IPFS 不得保存医疗明文。
3. 链上不得保存患者真实身份信息和医疗明文。
4. 所有合约函数需要权限控制。
5. 授权检查应同时在前端和合约层实现。
6. 合约应避免重入、越权、整数溢出、未校验地址等问题。
7. 前端应防止明显的 XSS 风险，不直接渲染未处理的用户输入。

### 8.2 隐私性

1. 链上仅保存哈希、状态和必要索引。
2. CID 建议加密后保存到后端或链下索引中，链上只保存 CID 哈希。
3. 元数据应最小化，避免包含姓名、身份证、手机号、详细诊断等敏感内容。
4. 对科研场景应进行脱敏处理。
5. 审计员只能看到行为记录，不能查看医疗内容。

### 8.3 可用性

1. 主要流程应在 3 到 5 个页面内完成。
2. 患者授权操作要清楚展示风险、范围和期限。
3. 医生查看数据时应显示授权状态和过期时间。
4. 合约交易等待期间需要显示 pending、success、failed 状态。
5. 钱包未连接、网络错误、IPFS 拉取失败、解密失败等状态应有明确提示。

### 8.4 性能

1. 单个医疗文件建议限制在 20MB 以内用于演示。
2. 前端列表查询优先从后端索引读取，再用链上数据校验关键状态。
3. 常用事件由后端索引到数据库，避免前端直接扫描全部链上事件。
4. 合约中避免存储大字符串和大数组。

### 8.5 可扩展性

1. 医疗数据类型可扩展。
2. 授权范围可从单资产扩展到批量资产。
3. 存储层可从 IPFS 替换为 Filecoin、Arweave 或私有 IPFS。
4. 身份层可扩展 DID/VC。
5. 隐私层可扩展零知识证明或属性基加密。

## 9. 总体架构设计

系统采用“链上合约 + 链下加密存储 + 非信任后端 + 前端 DApp”的混合架构。

```text
┌─────────────────────────────────────────────────────────────┐
│                         用户浏览器                           │
│  React DApp + Wallet + WebCrypto + 本地解密/加密/签名         │
└───────────────┬─────────────────────┬───────────────────────┘
                │                     │
                │ 合约调用/事件读取     │ REST/WebSocket
                │                     │
┌───────────────▼──────────────┐      │
│        Ethereum 网络          │      │
│ IdentityRegistry              │      │
│ MedicalDataRegistry           │      │
│ ConsentAccessControl          │      │
│ AuditLog/Event                │      │
└───────────────┬──────────────┘      │
                │ 事件监听             │
                │                     ▼
        ┌───────▼──────────────────────────────┐
        │          后端索引与通知服务            │
        │ Node.js/NestJS + PostgreSQL + Redis   │
        │ 事件索引、目录缓存、通知、IPFS Pinning │
        └──────────────┬───────────────────────┘
                       │ 上传/下载密文
                       ▼
              ┌─────────────────┐
              │ IPFS/Pinning    │
              │ 仅保存密文文件   │
              └─────────────────┘
```

### 9.1 架构设计原则

1. 区块链负责可信状态，不负责存储明文和大文件。
2. IPFS 负责存储加密文件，不负责权限判断。
3. 后端负责可用性，不成为可信中心。
4. 前端负责加解密和用户交互，是隐私保护的关键边界。
5. 智能合约负责身份、资产、授权和审计的不可篡改记录。

## 10. 前端设计

### 10.1 技术选型

| 类别 | 建议技术 |
| --- | --- |
| 前端框架 | React + TypeScript + Vite |
| 钱包连接 | wagmi + viem 或 ethers.js |
| UI | Tailwind CSS + shadcn/ui 或 Ant Design |
| 状态管理 | Zustand 或 React Query |
| 表单 | React Hook Form + Zod |
| 加密 | WebCrypto API + noble-curves/libsodium |
| 文件处理 | Browser File API |
| 图表/时间线 | ECharts 或 Recharts |

### 10.2 页面结构

```text
/login
  钱包连接、网络检测、角色识别

/dashboard
  根据角色展示待处理事项、数据统计、最近审计记录

/patient/data
  患者医疗数据列表、上传、分类、版本管理

/patient/requests
  访问申请审批、拒绝、授权设置

/patient/grants
  授权管理、撤销、过期状态

/doctor/request
  医生发起访问申请

/doctor/authorized
  医生查看已授权数据、解密查看

/research/projects
  研究项目申请、脱敏数据访问

/audit
  审计时间线、过滤、导出摘要

/admin
  医生/研究人员审核、机构管理
```

### 10.3 关键前端组件

| 组件 | 功能 |
| --- | --- |
| WalletGuard | 检查钱包连接、链 ID、用户角色 |
| RoleSwitcher | 根据钱包地址和链上身份显示角色入口 |
| DataUploadPanel | 文件选择、元数据填写、本地加密、上传 IPFS |
| MedicalDataTable | 数据资产列表、类别、版本、状态 |
| AccessRequestForm | 医生填写访问目的、范围和期限 |
| RequestReviewCard | 患者审批申请，显示申请人资质和风险提示 |
| GrantTimeline | 授权生命周期时间线 |
| AuditTimeline | 链上事件审计可视化 |
| EncryptedViewer | 解密并展示医疗文件或结构化记录 |
| TransactionToast | 交易状态提示 |
| NetworkBanner | 网络错误和测试网切换提示 |

### 10.4 前端状态设计

核心状态包括：

1. walletState：当前地址、链 ID、连接状态。
2. userState：角色、审核状态、加密公钥。
3. dataAssets：患者数据资产列表。
4. accessRequests：待处理申请、已发起申请。
5. grants：有效授权、过期授权、撤销授权。
6. auditEvents：审计事件时间线。
7. cryptoState：本地加密密钥是否可用、密钥恢复状态。

建议使用 React Query 管理后端 API 与链上读取缓存，使用 Zustand 管理钱包和本地 UI 状态。

### 10.5 前端交互细节

1. 首次登录时必须生成或导入加密密钥。
2. 发起上链交易前显示交易摘要，包括函数、对象、费用估计和风险提示。
3. 授权审批页面必须突出显示：
   - 申请人身份。
   - 数据范围。
   - 访问目的。
   - 授权期限。
   - 是否允许下载。
4. 数据查看页面必须显示水印：
   - 访问者钱包地址。
   - 授权 ID。
   - 当前时间。
5. 解密失败时区分：
   - 授权过期。
   - 密钥不可用。
   - 文件哈希不匹配。
   - IPFS 获取失败。
6. 所有角色界面共享统一导航，但不同角色展示不同菜单。

## 11. 后端设计

### 11.1 后端定位

后端不是数据可信中心，只作为非信任辅助服务。即使后端被攻击，攻击者也只能获得密文、哈希、公开事件和缓存数据，不能获得医疗明文。

### 11.2 技术选型

| 类别 | 建议技术 |
| --- | --- |
| 框架 | NestJS 或 Express |
| 数据库 | PostgreSQL |
| ORM | Prisma |
| 缓存/队列 | Redis，可选 |
| 区块链监听 | viem/ethers.js |
| 文件存储 | IPFS Kubo、本地 IPFS 节点或 Pinata/Web3.Storage |
| API 文档 | Swagger/OpenAPI |
| 部署 | Docker Compose |

### 11.3 后端模块

| 模块 | 功能 |
| --- | --- |
| EventIndexer | 监听合约事件并写入数据库 |
| DataCatalogService | 提供数据目录、资产列表、过滤查询 |
| NotificationService | 根据链上事件生成通知 |
| IpfsService | 上传密文、pin 文件、获取密文 |
| UserProfileService | 保存非敏感用户展示信息和审核缓存 |
| AuditQueryService | 提供审计事件聚合查询 |
| ResearchService | 脱敏项目和研究申请辅助管理 |

### 11.4 后端 API 设计

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /api/users/:address | 获取用户公开资料和审核状态 |
| GET | /api/assets?owner=address | 获取患者数据资产目录 |
| POST | /api/ipfs/upload | 上传密文文件到 IPFS |
| GET | /api/ipfs/:cid | 拉取密文文件 |
| GET | /api/requests?user=address | 查询访问申请 |
| GET | /api/grants?user=address | 查询授权记录 |
| GET | /api/audit?address=address | 查询审计事件 |
| GET | /api/notifications | 查询通知 |
| POST | /api/notifications/:id/read | 标记通知已读 |
| GET | /api/research/projects | 查询研究项目 |

后端 API 安全要求：

1. 需要钱包签名登录，后端使用签名校验地址所有权。
2. API 只返回用户有权查看的目录信息和审计索引。
3. 上传接口只接受密文文件，并记录 contentHash。
4. 后端不接受明文医疗字段。

### 11.5 数据库表设计

#### users

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| address | varchar | 钱包地址，主键 |
| role | varchar | 角色 |
| display_name | varchar | 展示名，不保存真实敏感身份 |
| organization_id | varchar | 所属机构 |
| public_key | text | 加密公钥 |
| status | varchar | pending/active/rejected |
| profile_hash | varchar | 链上资料哈希 |
| created_at | timestamp | 创建时间 |

#### data_assets

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| asset_id | varchar | 链上资产 ID |
| owner | varchar | 患者地址 |
| category | varchar | 数据类别 |
| cid_encrypted | text | 加密后的 CID 或后端密文引用 |
| cid_hash | varchar | CID 哈希 |
| content_hash | varchar | 密文哈希 |
| metadata_hash | varchar | 元数据哈希 |
| version | int | 版本号 |
| status | varchar | active/disabled |
| block_number | bigint | 区块号 |
| created_at | timestamp | 创建时间 |

#### access_requests

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| request_id | varchar | 链上申请 ID |
| requester | varchar | 申请人 |
| patient | varchar | 患者地址 |
| asset_scope | jsonb | 申请的数据范围 |
| purpose | varchar | 访问目的 |
| duration | int | 申请时长 |
| status | varchar | pending/approved/rejected/cancelled |
| created_at | timestamp | 创建时间 |

#### grants

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| grant_id | varchar | 授权 ID |
| request_id | varchar | 对应申请 |
| patient | varchar | 患者地址 |
| grantee | varchar | 被授权方 |
| scope | jsonb | 授权范围 |
| encrypted_key_ref | text | 加密 DataKey 的引用 |
| expires_at | timestamp | 过期时间 |
| status | varchar | active/revoked/expired |

#### audit_events

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| event_type | varchar | 事件类型 |
| tx_hash | varchar | 交易哈希 |
| actor | varchar | 操作者 |
| target | varchar | 目标地址或资产 |
| asset_id | varchar | 资产 ID |
| request_id | varchar | 申请 ID |
| grant_id | varchar | 授权 ID |
| block_number | bigint | 区块号 |
| created_at | timestamp | 事件时间 |

## 12. 智能合约设计

### 12.1 合约列表

| 合约 | 职责 |
| --- | --- |
| IdentityRegistry | 用户注册、角色、公钥、审核状态 |
| MedicalDataRegistry | 医疗数据资产登记、版本、状态 |
| ConsentAccessControl | 访问申请、授权、撤销、过期判断 |
| AuditEventEmitter | 统一事件定义或审计事件辅助 |

### 12.2 IdentityRegistry

核心数据结构：

```solidity
enum Role {
    None,
    Patient,
    Doctor,
    Researcher,
    InstitutionAdmin,
    Auditor
}

enum UserStatus {
    None,
    Pending,
    Active,
    Rejected,
    Suspended
}

struct UserProfile {
    address account;
    Role role;
    UserStatus status;
    bytes32 profileHash;
    bytes32 publicKeyHash;
    string publicKeyUri;
    address organization;
    uint256 createdAt;
    uint256 updatedAt;
}
```

核心函数：

```solidity
function registerUser(Role role, bytes32 profileHash, bytes32 publicKeyHash, string calldata publicKeyUri) external;
function approveUser(address account) external onlyInstitutionAdmin;
function rejectUser(address account) external onlyInstitutionAdmin;
function updatePublicKey(bytes32 publicKeyHash, string calldata publicKeyUri) external;
function getUser(address account) external view returns (UserProfile memory);
function isActiveDoctor(address account) external view returns (bool);
```

关键事件：

```solidity
event UserRegistered(address indexed account, Role role, bytes32 profileHash);
event UserApproved(address indexed account, address indexed approver);
event UserRejected(address indexed account, address indexed approver);
event PublicKeyUpdated(address indexed account, bytes32 publicKeyHash);
```

### 12.3 MedicalDataRegistry

核心数据结构：

```solidity
enum DataStatus {
    Active,
    Disabled
}

struct DataAsset {
    uint256 assetId;
    address owner;
    bytes32 cidHash;
    bytes32 contentHash;
    bytes32 metadataHash;
    bytes32 category;
    uint256 version;
    DataStatus status;
    uint256 createdAt;
}
```

核心函数：

```solidity
function registerDataAsset(
    bytes32 cidHash,
    bytes32 contentHash,
    bytes32 metadataHash,
    bytes32 category
) external returns (uint256 assetId);

function createNewVersion(
    uint256 previousAssetId,
    bytes32 cidHash,
    bytes32 contentHash,
    bytes32 metadataHash
) external returns (uint256 newAssetId);

function disableAsset(uint256 assetId) external;
function getAsset(uint256 assetId) external view returns (DataAsset memory);
function isOwner(uint256 assetId, address account) external view returns (bool);
```

关键事件：

```solidity
event DataAssetRegistered(uint256 indexed assetId, address indexed owner, bytes32 category);
event DataAssetVersioned(uint256 indexed oldAssetId, uint256 indexed newAssetId);
event DataAssetDisabled(uint256 indexed assetId, address indexed owner);
```

### 12.4 ConsentAccessControl

核心数据结构：

```solidity
enum RequestStatus {
    Pending,
    Approved,
    Rejected,
    Cancelled
}

enum GrantStatus {
    Active,
    Revoked,
    Expired
}

struct AccessRequest {
    uint256 requestId;
    address requester;
    address patient;
    uint256[] assetIds;
    bytes32 purpose;
    uint256 requestedDuration;
    RequestStatus status;
    uint256 createdAt;
}

struct AccessGrant {
    uint256 grantId;
    uint256 requestId;
    address patient;
    address grantee;
    uint256[] assetIds;
    bytes32 encryptedKeyHash;
    string encryptedKeyUri;
    uint256 expiresAt;
    GrantStatus status;
    uint256 createdAt;
}
```

核心函数：

```solidity
function createAccessRequest(
    address patient,
    uint256[] calldata assetIds,
    bytes32 purpose,
    uint256 requestedDuration
) external returns (uint256 requestId);

function approveRequest(
    uint256 requestId,
    string calldata encryptedKeyUri,
    bytes32 encryptedKeyHash,
    uint256 expiresAt
) external returns (uint256 grantId);

function rejectRequest(uint256 requestId, string calldata reasonUri) external;
function revokeGrant(uint256 grantId) external;
function hasValidAccess(address grantee, uint256 assetId) external view returns (bool);
function recordAccess(uint256 grantId, uint256 assetId, bytes32 accessProofHash) external;
```

关键事件：

```solidity
event AccessRequested(uint256 indexed requestId, address indexed requester, address indexed patient);
event AccessApproved(uint256 indexed requestId, uint256 indexed grantId, address indexed grantee);
event AccessRejected(uint256 indexed requestId, address indexed patient);
event GrantRevoked(uint256 indexed grantId, address indexed patient);
event DataAccessRecorded(uint256 indexed grantId, uint256 indexed assetId, address indexed accessor);
```

### 12.5 合约安全要求

1. 使用 OpenZeppelin AccessControl、Ownable、Pausable。
2. 关键写操作必须检查角色和状态。
3. registerDataAsset 只允许 Active Patient 调用。
4. approveRequest 只允许目标 patient 调用。
5. revokeGrant 只允许 patient 或合规管理员在特殊条件下调用。
6. hasValidAccess 必须检查授权状态、资产范围、过期时间。
7. 不能在合约中保存医疗明文、明文 CID 和明文密钥。
8. 数组长度要限制，避免一次授权过多资产导致 gas 过高。

## 13. 加密与隐私设计

### 13.1 混合加密方案

系统采用“对称加密数据 + 非对称加密密钥”的混合加密方式。

上传时：

1. 前端生成随机 DataKey。
2. 用 AES-GCM 加密医疗文件。
3. 用患者加密公钥封装 DataKey。
4. 上传密文文件到 IPFS。
5. 链上保存哈希和资产状态。

授权时：

1. 患者前端解封装 DataKey。
2. 读取医生或研究人员的加密公钥。
3. 用被授权方公钥重新封装 DataKey。
4. 将封装后的密钥密文保存到 IPFS 或后端密文存储。
5. 链上保存 encryptedKeyHash 和 encryptedKeyUri。

访问时：

1. 被授权方检查链上 Grant 是否有效。
2. 获取加密 DataKey。
3. 用自己的私钥解封装 DataKey。
4. 从 IPFS 获取密文。
5. 校验 contentHash。
6. 本地解密并展示。

### 13.2 密钥管理

推荐实现：

1. 用户首次登录时生成加密密钥对。
2. 私钥使用从钱包签名派生的本地密钥加密后保存到浏览器 IndexedDB。
3. 公钥上传到后端并将哈希登记到合约。
4. 换浏览器时需要导出/导入加密私钥，或重新生成公钥并更新链上记录。

注意：

1. 不建议直接依赖 MetaMask 的历史加解密接口，因为不同钱包支持不一致。
2. 私钥丢失会导致历史数据无法解密，应在毕业设计中说明密钥备份机制。
3. 可以设计“恢复助记词导出”或“患者重新上传数据版本”的补救方案。

### 13.3 哈希与完整性校验

1. contentHash = SHA-256(ciphertext)。
2. metadataHash = SHA-256(canonicalMetadataJson)。
3. cidHash = keccak256(CID) 或 SHA-256(CID)。
4. 前端下载密文后必须重新计算 contentHash，与链上值比对。
5. 若哈希不一致，必须阻止解密展示。

### 13.4 链上隐私约束

禁止上链字段：

1. 患者姓名。
2. 身份证号。
3. 手机号。
4. 家庭住址。
5. 详细诊断结果。
6. 明文病历内容。
7. 明文 IPFS CID。
8. 明文 DataKey。

允许上链字段：

1. 钱包地址。
2. 哈希值。
3. 角色枚举。
4. 授权状态。
5. 访问目的枚举哈希。
6. 时间戳。
7. 事件日志。

### 13.5 高级隐私扩展

可根据毕业设计时间选择 1 个实现：

1. DID/VC：医院给医生签发可验证凭证，患者审批时验证医生资质。
2. 零知识证明：医生证明自己属于某医院或某科室，但不公开完整身份信息。
3. 属性基加密：按“医生 + 科室 + 机构”属性授权。
4. 门限紧急访问：急救摘要需要多方签名才能打开。
5. 访问水印：数据查看页面嵌入访问者地址和授权 ID，降低截图传播风险。

毕业设计建议优先实现 DID/VC 或访问水印，因为可演示性强，复杂度可控。

## 14. 数据结构与医疗模型设计

系统不直接完整实现 HL7 FHIR，但采用 FHIR 风格字段，便于论文说明与扩展。

### 14.1 医疗数据元数据

```json
{
  "assetId": "链上生成",
  "category": "LAB_REPORT",
  "title": "2026-05 血常规报告",
  "hospital": "示例医院",
  "department": "检验科",
  "recordDate": "2026-05-01",
  "tags": ["blood", "routine"],
  "schemaVersion": "1.0"
}
```

### 14.2 结构化检验报告示例

```json
{
  "resourceType": "ObservationBundle",
  "patientRef": "did:ethr:0x...",
  "observations": [
    {
      "code": "WBC",
      "display": "白细胞计数",
      "value": 6.2,
      "unit": "10^9/L",
      "referenceRange": "3.5-9.5",
      "status": "normal"
    },
    {
      "code": "HGB",
      "display": "血红蛋白",
      "value": 142,
      "unit": "g/L",
      "referenceRange": "130-175",
      "status": "normal"
    }
  ]
}
```

### 14.3 访问目的枚举

| 编码 | 说明 |
| --- | --- |
| TREATMENT | 诊疗 |
| FOLLOW_UP | 复诊 |
| CONSULTATION | 会诊 |
| RESEARCH | 科研 |
| EMERGENCY | 紧急救治 |

## 15. 主要用例

### 15.1 用例 UC-01 患者上传检验报告

参与者：患者  
前置条件：患者已注册并拥有加密公钥  
主流程：

1. 患者选择检验报告文件。
2. 填写报告类别、医院、日期和标签。
3. 前端本地加密文件。
4. 上传密文到 IPFS。
5. 调用合约登记数据资产。
6. 系统展示上传成功和链上交易哈希。

后置条件：链上存在数据资产记录，IPFS 存储密文。

### 15.2 用例 UC-02 医生申请访问患者数据

参与者：医生、患者  
前置条件：医生已通过审核，患者已有数据资产  
主流程：

1. 医生输入患者分享码或地址。
2. 选择需要访问的数据资产。
3. 填写访问目的和期限。
4. 调用合约创建申请。
5. 患者收到申请通知。
6. 患者审批并封装数据密钥。
7. 合约生成授权记录。
8. 医生解密查看数据。

后置条件：系统记录完整申请和授权审计。

### 15.3 用例 UC-03 患者撤销授权

参与者：患者  
前置条件：存在有效授权  
主流程：

1. 患者进入授权管理。
2. 选择某个授权。
3. 查看授权详情和最近访问记录。
4. 点击撤销。
5. 钱包签名并发送交易。
6. 合约更新 Grant 状态。
7. 医生端无法继续通过系统访问。

后置条件：授权状态变为 Revoked，审计记录新增撤销事件。

### 15.4 用例 UC-04 审计员查询访问记录

参与者：审计员  
前置条件：审计员已注册并通过授权  
主流程：

1. 审计员进入审计中心。
2. 按患者地址、医生地址、资产 ID 或时间范围过滤。
3. 系统从后端索引读取事件。
4. 审计员查看交易哈希和链上状态。
5. 可导出摘要报告。

后置条件：审计员完成行为追溯，不接触医疗明文。

## 16. UI 原型文字说明

### 16.1 患者仪表盘

展示内容：

1. 医疗数据数量。
2. 待审批申请数量。
3. 有效授权数量。
4. 最近访问记录。
5. 快捷入口：上传数据、处理申请、查看审计。

设计重点：

1. 患者最关心“谁正在请求我的数据”和“谁已经可以访问我的数据”。
2. 授权和撤销按钮要明显。
3. 数据状态用颜色区分：有效、已停用、已授权、存在风险。

### 16.2 医生仪表盘

展示内容：

1. 已授权患者数量。
2. 待患者审批申请。
3. 即将过期授权。
4. 最近查看数据。
5. 快捷入口：发起申请、查看授权数据。

设计重点：

1. 医生端强调效率和授权有效性。
2. 数据查看页面不能让医生误以为数据可永久使用。
3. 每份数据展示访问目的、期限和水印。

### 16.3 审计页面

展示内容：

1. 事件时间线。
2. 交易哈希。
3. 事件类型。
4. 操作者。
5. 目标资产或授权。
6. 区块高度。

设计重点：

1. 审计页面应支持快速过滤。
2. 每条事件可跳转到区块浏览器。
3. 事件顺序要与业务流程对应。

## 17. 错误处理

| 场景 | 处理方式 |
| --- | --- |
| 钱包未连接 | 引导连接钱包 |
| 网络错误 | 提示切换到指定测试网 |
| 用户未注册 | 跳转注册页面 |
| 医生未审核 | 禁止发起访问申请 |
| 上传 IPFS 失败 | 保留本地加密结果，允许重试 |
| 合约交易失败 | 显示失败原因和交易哈希 |
| 授权已过期 | 禁止解密，提示重新申请 |
| 数据哈希不一致 | 阻止展示，提示数据完整性异常 |
| 私钥丢失 | 提示导入备份密钥或重新生成密钥 |

## 18. 测试与验收标准

### 18.1 智能合约测试

必须覆盖：

1. 患者注册成功。
2. 医生注册后未审核不能申请访问。
3. 患者上传数据资产成功。
4. 非所有者不能禁用他人数据。
5. 医生提交访问申请成功。
6. 非目标患者不能审批申请。
7. 授权过期后 hasValidAccess 返回 false。
8. 撤销授权后无法继续访问。
9. 事件参数正确。
10. 异常输入会 revert。

### 18.2 前端测试

至少覆盖：

1. 钱包连接状态切换。
2. 上传表单校验。
3. 加密和哈希计算流程。
4. 授权审批交互。
5. 授权撤销交互。
6. 解密失败提示。

### 18.3 后端测试

至少覆盖：

1. 合约事件监听。
2. 事件重复处理幂等性。
3. 数据目录查询。
4. 通知生成。
5. API 权限校验。

### 18.4 系统验收流程

答辩演示建议流程：

1. 启动本地链、部署合约。
2. 注册患者 Alice。
3. 注册医生 Bob，并由管理员审核。
4. Alice 上传血检报告，系统显示链上资产 ID。
5. Bob 申请访问 Alice 的血检报告。
6. Alice 审批授权 7 天。
7. Bob 查看并解密报告。
8. Alice 查看审计记录。
9. Alice 撤销授权。
10. Bob 再次访问失败。

验收标准：

1. 主流程可完整跑通。
2. 区块链交易可查询。
3. IPFS 中只有密文。
4. 后端数据库无医疗明文。
5. 审计记录完整。
6. 合约测试全部通过。

## 19. 开发计划

### 19.1 第一阶段：基础框架

1. 初始化前端、后端、合约项目。
2. 实现钱包连接。
3. 实现用户注册合约。
4. 实现本地链部署脚本。

### 19.2 第二阶段：数据资产与加密上传

1. 实现客户端 AES-GCM 加密。
2. 接入 IPFS 上传密文。
3. 实现 MedicalDataRegistry。
4. 前端展示患者数据列表。

### 19.3 第三阶段：访问申请与授权

1. 实现 ConsentAccessControl。
2. 医生发起申请。
3. 患者审批和密钥封装。
4. 医生解密查看。

### 19.4 第四阶段：审计与后端索引

1. 后端监听合约事件。
2. 数据库保存索引。
3. 实现通知和审计页面。
4. 支持授权撤销。

### 19.5 第五阶段：高级功能与论文材料

1. 实现研究数据脱敏或紧急访问。
2. 增加访问水印。
3. 补充合约测试和前端测试。
4. 整理系统截图、流程图和实验数据。

## 20. 技术难点与解决方案

| 难点 | 解决方案 |
| --- | --- |
| 医疗数据不能明文上链 | 只上链哈希、状态和授权记录 |
| IPFS 内容公开可访问 | 上传前进行客户端加密 |
| 授权撤销无法删除已下载数据 | 使用授权过期、撤销、审计、水印、密钥轮换说明边界 |
| 钱包不统一支持解密 | 使用浏览器生成加密密钥对，钱包只负责身份签名 |
| 链上查询效率低 | 后端监听事件建立索引 |
| 医生资质难以真实验证 | 毕设阶段用管理员审核模拟，扩展 DID/VC |
| Gas 成本高 | 大文件链下存储，合约只保存必要字段 |
| 用户密钥丢失 | 提供密钥导出/导入或重新上传机制 |

## 21. 毕业设计创新点

1. 数据所有权与授权控制结合：患者通过智能合约管理医疗数据访问权。
2. 链上链下协同：链上保存可信状态，链下保存加密医疗数据，兼顾成本和隐私。
3. 客户端端到端加密：后端和存储网络无法读取明文。
4. 全流程审计：申请、授权、访问、撤销均可追溯。
5. 细粒度授权：支持按数据资产、类别、期限和目的授权。
6. 非信任后端架构：后端提高可用性，但不破坏去中心化信任模型。
7. 可扩展身份隐私：预留 DID/VC、零知识证明、紧急访问等高级模块。

## 22. 当前设计问题与优化方案

本节用于说明系统设计中容易被导师或评审质疑的问题，以及本文档采用的修正方案。

### 22.1 问题一：链上地址关系会暴露访问关系

问题说明：

即使医疗数据明文不上链，患者地址、医生地址、访问申请、授权时间等链上事件仍然公开。外部观察者可能通过地址关系推测某患者与某医生或机构存在医疗关联。

优化方案：

1. 毕业设计默认使用模拟地址和测试数据，不绑定真实身份。
2. 患者可以生成一次性分享码，由后端映射到患者地址，避免医生直接输入患者真实身份信息。
3. 论文中明确说明链上元数据隐私问题，作为系统限制。
4. 高级扩展可引入中继合约、匿名凭证、零知识证明或隐私链，但不放入核心实现范围。

### 22.2 问题二：授权撤销不能删除已泄露数据

问题说明：

区块链可以撤销系统内访问权限，但无法让已经解密、下载、截图或复制的数据自动消失。

优化方案：

1. 授权撤销只承诺阻止后续系统内访问。
2. 数据查看页加入访问水印，包含访问者地址、授权 ID、时间戳。
3. 对敏感文件默认只提供在线预览，不提供直接明文下载按钮。
4. 审计日志记录访问行为，用于事后追责。
5. 论文中将其归类为“技术边界与合规治理问题”。

### 22.3 问题三：IPFS 不能天然保证隐私和长期可用

问题说明：

IPFS 的 CID 如果公开，任何人都可以尝试拉取对应内容。虽然内容是密文，但 CID 本身也可能成为一种元数据。另一方面，如果没有节点 pin 文件，IPFS 内容可能不可用。

优化方案：

1. 上传 IPFS 的内容必须是密文。
2. 链上只保存 CID 哈希，不保存明文 CID。
3. 明文 CID 或加密 CID 存放在后端索引或密钥包中，并按授权返回。
4. 后端提供 IPFS pinning 服务，保证演示数据可用。
5. 验收时检查 IPFS 文件内容不可直接读懂。

### 22.4 问题四：每次读取都上链记录成本较高

问题说明：

如果医生每次打开数据都调用 recordAccess 上链，会产生 gas 成本和等待时间，不适合频繁读取。

优化方案：

1. 核心链上审计记录申请、审批、撤销等关键状态变更。
2. 数据读取记录可分为两级：
   - 关键读取确认：首次读取上链记录。
   - 普通读取日志：后端签名记录并定期生成哈希摘要上链。
3. 毕业设计 MVP 实现“首次读取上链 + 后端普通日志”即可。
4. 论文中说明这是成本和审计完整性之间的工程取舍。

### 22.5 问题五：后端虽然非信任，但仍影响可用性

问题说明：

系统宣称后端非信任，并不代表后端不重要。后端如果宕机，通知、索引、搜索和 IPFS pinning 会受影响。

优化方案：

1. 后端不保存明文和私钥，因此不能破坏数据机密性。
2. 关键授权状态以智能合约为准，后端索引可以重建。
3. 前端在必要时可以直接读取合约状态作为兜底。
4. 后端数据库保存 lastProcessedBlock，用于故障恢复后继续索引事件。
5. 毕业设计中应提供“重新同步链上事件”的脚本或接口。

### 22.6 问题六：密钥丢失会导致历史数据不可恢复

问题说明：

端到端加密系统中，如果患者加密私钥丢失，历史 DataKey 无法解封装，数据即使还在 IPFS 上也无法恢复明文。

优化方案：

1. 首次生成密钥时强制提示用户导出加密备份。
2. 私钥备份文件使用用户设置的恢复密码加密。
3. 系统提供“导入密钥备份”功能。
4. 若无法恢复，只能重新上传数据并生成新版本。
5. 论文中将密钥备份作为系统可用性的关键设计。

### 22.7 问题七：医生资质认证真实度不足

问题说明：

毕业设计无法真实接入卫健委、医院 HIS 或执业医师认证系统，因此医生身份审核可能被认为过于简单。

优化方案：

1. MVP 使用机构管理员审核模拟真实资质认证。
2. 增强版使用可验证凭证 VC：机构给医生签发“医生资质凭证”。
3. 前端审批页展示医生所属机构、科室、审核状态和凭证哈希。
4. 论文中说明真实系统可对接医院 IAM、DID/VC 或第三方认证平台。

### 22.8 问题八：科研脱敏容易被重新识别

问题说明：

简单删除姓名和手机号并不一定安全。年龄、病种、地区、时间等组合字段仍可能重新识别个人。

优化方案：

1. 毕业设计中的科研共享只作为增强功能，不处理真实数据。
2. 脱敏规则至少包括年龄区间化、日期粗粒度化、稀有标签隐藏。
3. 小样本数据不允许导出，避免重新识别风险。
4. 论文中说明可扩展 k-匿名、差分隐私等方法。

## 23. 建议最终实现范围

为了保证毕业设计既不简单又能完成，建议按以下范围实现。

### 23.1 必须实现

1. 钱包登录。
2. 角色注册。
3. 医生审核。
4. 患者上传加密医疗文件。
5. IPFS 存储密文。
6. 链上登记数据资产。
7. 医生申请访问。
8. 患者审批授权。
9. 医生解密查看。
10. 患者撤销授权。
11. 审计日志展示。
12. 合约单元测试。

### 23.2 建议实现

1. 数据版本管理。
2. 后端事件索引。
3. 通知中心。
4. 访问水印。
5. 研究数据脱敏共享。

### 23.3 可选实现

1. DID/VC 医生资质。
2. 零知识证明。
3. 紧急访问。
4. 审计报告导出。
5. Docker Compose 一键启动。

### 23.4 分层落地建议

为了避免范围过大，建议按三层交付。

| 层级 | 名称 | 功能范围 | 适用目标 |
| --- | --- | --- | --- |
| L1 | 最小可验收版本 | 钱包登录、角色注册、患者上传密文、链上登记、医生申请、患者授权、医生解密、撤销授权 | 保证毕业设计主流程跑通 |
| L2 | 完整毕业设计版本 | L1 + 后端事件索引、通知、审计页面、数据版本、访问水印、合约测试 | 推荐最终答辩版本 |
| L3 | 加分扩展版本 | L2 + 科研脱敏、DID/VC、紧急访问、审计报告导出 | 时间充足时实现 |

推荐实现到 L2。L3 只选择 1 到 2 个功能，不建议全部实现。

### 23.5 推荐删减或延后内容

以下内容虽然高级，但不建议全部作为核心需求：

1. 零知识证明：理论性强，实现和调试成本高，除非论文重点就是 ZKP。
2. 属性基加密：适合研究型课题，但毕业设计工程实现成本较高。
3. 真实医院系统接入：接口、数据合规和环境成本不可控。
4. 可交易代币激励：会偏离医疗数据共享主题，还可能引入不必要的监管讨论。
5. 完整移动端 App：响应式 Web DApp 已足够支撑演示。

## 24. 论文写作对应章节建议

1. 绪论：医疗数据共享问题、区块链优势、研究意义。
2. 相关技术：以太坊、智能合约、IPFS、混合加密、DID、访问控制。
3. 需求分析：用户角色、功能需求、非功能需求、用例图。
4. 系统设计：总体架构、合约设计、后端设计、前端设计、数据加密流程。
5. 系统实现：关键模块代码说明、界面展示、流程实现。
6. 测试与分析：合约测试、功能测试、安全性分析、性能分析。
7. 总结与展望：系统成果、不足、未来扩展。

## 25. 答辩可能追问与建议回答

### 25.1 为什么不把医疗数据直接上链？

建议回答：

医疗数据体积大、隐私敏感，而且链上数据难以删除。系统只将哈希、授权状态和审计事件上链，明文数据在客户端加密后存储到 IPFS，从而兼顾可信审计和隐私保护。

### 25.2 后端存在是否违背去中心化？

建议回答：

后端只承担索引、通知、pinning 和查询优化，不保存明文数据，也不能改变链上授权状态。系统可信状态以智能合约为准，因此后端是可替换、可重建的辅助组件。

### 25.3 患者撤销授权后，医生已经下载的数据怎么办？

建议回答：

撤销机制只能阻止未来通过系统访问，无法删除已经复制到系统外的数据。系统通过授权期限、访问水印、审计追踪和责任追溯降低滥用风险。这是所有数字数据共享系统都需要面对的边界。

### 25.4 如何保证医生身份可信？

建议回答：

毕业设计阶段通过机构管理员审核模拟真实资质认证。系统预留 DID/VC 扩展接口，真实应用中可由医院或监管机构签发医生资质凭证。

### 25.5 如果患者私钥丢失怎么办？

建议回答：

系统提供加密私钥导出和导入功能。若用户没有备份私钥，则历史加密数据无法恢复，只能重新上传新版本。这体现了端到端加密系统中密钥管理的重要性。

### 25.6 为什么使用 IPFS？

建议回答：

IPFS 适合存储不可变内容，CID 可以和链上哈希配合完成完整性校验。系统上传的是密文，即使 IPFS 内容被公开获取，也不能直接读取医疗明文。

### 25.7 系统是否满足医疗合规要求？

建议回答：

毕业设计系统使用模拟或脱敏数据，不作为真实医疗系统上线。设计上参考最小化数据、端到端加密、授权审计等隐私保护原则，但真实上线仍需满足当地医疗数据法规、安全测评和机构合规流程。

## 26. 关键验收指标

| 指标 | 验收方式 | 合格标准 |
| --- | --- | --- |
| 主流程完整性 | 按演示脚本执行 | 上传、申请、授权、解密、撤销、审计全部成功 |
| 数据隐私 | 检查数据库、IPFS、链上数据 | 不出现医疗明文和明文 DataKey |
| 完整性校验 | 篡改 IPFS 密文后尝试查看 | 前端能检测哈希不一致并拒绝展示 |
| 授权控制 | 使用未授权账号访问数据 | 无法获取密钥或无法解密 |
| 撤销控制 | 撤销后再次访问 | 系统拒绝后续访问 |
| 审计可追溯 | 查看审计页面和交易哈希 | 关键事件可按时间线追踪 |
| 合约安全 | 运行合约单元测试 | 关键权限和异常分支测试通过 |
| 可恢复性 | 重建后端索引 | 能从指定区块重新同步事件 |

## 27. 实施前最终确认清单

在正式编码前，建议确认以下 8 个问题：

1. 学校是否允许使用 Solidity、React、Node.js 作为毕业设计技术栈。
2. 是否要求必须有后端，还是纯 DApp 也可以。
3. 是否要求部署到公开测试网，若要求，需要提前准备测试币和 RPC。
4. 是否需要论文中加入算法或模型创新，若需要可选择脱敏算法、访问控制模型或 DID/VC。
5. 是否需要真实数据库表和接口文档，若需要建议保留 PostgreSQL 和 Swagger。
6. 是否要求系统截图和演示视频，若要求需要提前规划演示数据。
7. 是否需要性能测试，若需要重点测试加密耗时、IPFS 上传耗时、合约 gas。
8. 是否要求中文论文中的“需求分析”有用例图、类图、时序图，若要求后续应补 UML 图。

## 28. 默认结论

本系统采用以太坊智能合约实现身份、数据资产、授权和审计的可信管理，采用 IPFS 存储加密医疗数据，采用前端客户端加密保证医疗隐私，采用后端索引服务提高可用性。系统围绕患者、医生、研究人员、机构管理员和审计员形成完整业务闭环，既体现区块链不可篡改、可追溯和去中心化授权的特点，又避免将隐私数据直接上链的工程风险，适合作为毕业设计项目进行实现和论文撰写。

## 29. 参考依据

1. Ethereum.org Networks 文档：用于确认以太坊主网、测试网、本地开发网络的定位，以及 Sepolia 与 Hoodi 的推荐使用场景。文档地址：https://ethereum.org/developers/docs/networks/
2. OpenZeppelin Contracts 文档：用于智能合约权限控制、暂停机制和安全组件选型。文档地址：https://docs.openzeppelin.com/contracts/
3. IPFS 官方文档：用于去中心化内容寻址存储、CID 和 pinning 机制说明。文档地址：https://docs.ipfs.tech/
4. W3C Decentralized Identifiers 与 Verifiable Credentials 文档：用于 DID/VC 扩展方案说明。文档地址：https://www.w3.org/TR/did-core/ 和 https://www.w3.org/TR/vc-data-model/
5. Web Cryptography API 文档：用于浏览器端 AES-GCM 加密、随机数和密钥处理说明。文档地址：https://www.w3.org/TR/WebCryptoAPI/
