# MedLedger 使用说明

项目名称：基于以太坊的去中心化隐私医疗数据共享 DApp  
项目目录：`D:\medical`  
前端地址：`http://localhost:5173`  
后端地址：`http://localhost:4100`

## 1. 项目简介

本项目是一个面向毕业设计的医疗数据共享 DApp 原型，实现了“患者控制数据、医生按授权访问、关键行为链上审计、医疗数据链下加密存储”的完整流程。

系统采用：

- Ethereum Sepolia 测试网：保存身份、数据资产、访问申请、授权和撤销状态。
- Solidity 智能合约：实现链上访问控制。
- React + TypeScript：实现前端 DApp。
- MetaMask：负责钱包连接和交易签名。
- Express + TypeScript：实现后端演示 API、模拟 IPFS、通知和审计索引。
- WebCrypto AES-GCM：在浏览器中完成医疗数据加密和解密演示。

## 2. 已部署合约

网络：Sepolia  
链 ID：`11155111`  
部署账户：`0x9fA74A5ae1B9E68BDa86E8655c63Eb4af7f11A90`

| 合约 | 地址 |
| --- | --- |
| IdentityRegistry | `0xe6DF2a398A09c0C0ED23CEF4219322060c35C090` |
| MedicalDataRegistry | `0x037601068F0aAC93FbDb631c746D5f347C999661` |
| ConsentAccessControl | `0xFC96D31921Ea9477B5dC923912e4655f7e5C693f` |

区块浏览器：

- https://sepolia.etherscan.io/address/0xe6DF2a398A09c0C0ED23CEF4219322060c35C090
- https://sepolia.etherscan.io/address/0x037601068F0aAC93FbDb631c746D5f347C999661
- https://sepolia.etherscan.io/address/0xFC96D31921Ea9477B5dC923912e4655f7e5C693f

## 3. 项目结构

```text
D:\medical
├─ apps
│  ├─ api          后端 API，负责模拟 IPFS、通知、审计索引
│  └─ web          前端 DApp，负责 MetaMask、加密、授权和界面
├─ contracts       Solidity 合约、部署脚本、测试脚本
├─ design-system   UI 设计系统说明
├─ DEPLOYMENT.md   部署说明
├─ README.md       项目简介和基础命令
└─ 使用说明.md     当前使用说明文档
```

## 4. 环境要求

需要安装：

- Node.js 20 或更高版本
- npm
- Chrome 或 Edge 浏览器
- MetaMask 浏览器插件

本机已验证版本：

- Node.js：`v22.11.0`
- npm：`10.9.0`

## 5. 安装依赖

在项目根目录执行：

```powershell
cd D:\medical
npm.cmd install
```

如果已经安装过依赖，可以跳过这一步。

## 6. 启动项目

建议打开两个 PowerShell 窗口。

第一个窗口启动后端：

```powershell
cd D:\medical
npm.cmd run dev:api
```

后端启动后访问：

```text
http://localhost:4100/api/health
```

如果看到类似下面的内容，说明后端正常：

```json
{
  "status": "ok",
  "service": "medical-data-sharing-api"
}
```

第二个窗口启动前端：

```powershell
cd D:\medical
npm.cmd run dev:web
```

前端访问：

```text
http://localhost:5173
```

## 7. MetaMask 配置

1. 浏览器安装 MetaMask。
2. 切换到 Sepolia 测试网。
3. 如果没有 Sepolia 网络，可在前端点击“切换到 Sepolia”，系统会尝试自动添加。
4. 钱包中需要有少量 Sepolia ETH，用于支付测试网 gas。

Sepolia RPC 默认使用：

```text
https://ethereum-sepolia-rpc.publicnode.com
```

## 8. 演示流程

### 8.1 患者上传医疗数据

1. 打开 `http://localhost:5173`。
2. 点击“连接 MetaMask”。
3. 点击“切换到 Sepolia”。
4. 当前业务角色选择 `患者 Patient`。
5. 点击“登记链上身份”，在 MetaMask 中确认交易。
6. 点击“加密上传样例血检”。
7. 系统会在浏览器中加密样例血检数据。
8. MetaMask 会弹出交易确认窗口。
9. 确认后，数据资产摘要会登记到 Sepolia 合约。
10. 后端索引中会出现对应数据资产。

### 8.2 医生申请访问

1. 切换到医生钱包。
2. 当前业务角色选择 `医生 Doctor`。
3. 点击“登记链上身份”。
4. 管理员审批医生身份。
5. 医生进入“医生访问”页面。
6. 点击“发起申请”。
7. MetaMask 确认访问申请交易。
8. 患者端会看到待审批申请。

### 8.3 管理员审批医生身份

医生注册后，合约要求机构管理员审批，医生才可以发起访问申请。

使用命令审批医生：

```powershell
cd D:\medical
$env:SEPOLIA_RPC_URL='https://ethereum-sepolia-rpc.publicnode.com'
$env:DEPLOYER_PRIVATE_KEY='0x你的测试网管理员私钥'
$env:APPROVE_USER_ADDRESS='0x医生钱包地址'
npm.cmd --workspace contracts run approve:sepolia
```

注意：不要把私钥写入文件。

### 8.4 患者审批授权

1. 切回患者钱包。
2. 进入“申请审批”页面。
3. 查看医生、访问目的、申请范围和期限。
4. 点击“授权 7 天”。
5. MetaMask 确认授权交易。
6. 授权成功后，医生可访问对应数据。

### 8.5 医生解密查看数据

1. 切回医生钱包。
2. 进入“医生访问”页面。
3. 选择已授权数据。
4. 点击“校验并解密”。
5. 系统先校验密文哈希。
6. 校验通过后在浏览器中解密展示样例血检报告。
7. 页面会显示访问水印。

### 8.6 患者撤销授权

1. 切回患者钱包。
2. 进入“授权管理”页面。
3. 找到对应授权。
4. 点击“撤销授权”。
5. MetaMask 确认撤销交易。
6. 撤销后医生无法继续通过系统访问该数据。

### 8.7 查看审计记录

进入“审计中心”页面，可以查看：

- 用户登记
- 数据资产登记
- 访问申请
- 授权审批
- 数据访问
- 授权撤销

## 9. 常用命令

安装依赖：

```powershell
npm.cmd install
```

启动前端：

```powershell
npm.cmd run dev:web
```

启动后端：

```powershell
npm.cmd run dev:api
```

运行合约测试：

```powershell
npm.cmd --workspace contracts run test
```

构建前端：

```powershell
npm.cmd --workspace apps/web run build
```

构建后端：

```powershell
npm.cmd --workspace apps/api run build
```

重新部署 Sepolia 合约：

```powershell
$env:SEPOLIA_RPC_URL='https://ethereum-sepolia-rpc.publicnode.com'
$env:DEPLOYER_PRIVATE_KEY='0x你的测试网私钥'
npm.cmd --workspace contracts run deploy:sepolia
```

## 10. 当前实现范围

已实现：

- MetaMask 钱包连接
- Sepolia 网络切换
- 链上身份登记
- 链上医疗数据资产登记
- 链上访问申请
- 链上授权审批
- 链上授权撤销
- 本地 AES-GCM 加密演示
- 模拟 IPFS 密文存储
- 后端通知和审计索引
- 医疗数据解密查看
- 访问水印
- 合约测试

当前仍属于毕业设计原型，未完全实现生产级能力：

- 后端使用内存存储，重启后数据会丢失。
- IPFS 当前为模拟存储，未接入真实 pinning 服务。
- 医疗数据使用样例数据，不能上传真实病历。
- 密钥封装为演示流程，生产环境需要 X25519/ECIES 密钥封装。
- 医生审批目前通过命令完成，后续可补管理员 UI。

## 11. 常见问题

### 11.1 前端打不开

确认前端服务是否启动：

```powershell
npm.cmd run dev:web
```

然后访问：

```text
http://localhost:5173
```

### 11.2 后端接口失败

确认后端服务是否启动：

```powershell
npm.cmd run dev:api
```

检查：

```text
http://localhost:4100/api/health
```

### 11.3 MetaMask 无法发交易

检查：

- 是否连接钱包。
- 是否切换到 Sepolia。
- 钱包是否有 Sepolia ETH。
- 是否已经登记对应角色身份。
- 医生是否已经被管理员审批。

### 11.4 上传数据时合约报错

常见原因：

- 当前钱包没有登记为患者。
- 当前网络不是 Sepolia。
- MetaMask 拒绝交易。
- 合约地址不是最新部署地址。

### 11.5 医生发起申请失败

常见原因：

- 医生钱包没有登记身份。
- 医生身份尚未被管理员审批。
- 患者没有上传任何数据资产。
- 申请的数据资产 ID 不存在或不属于该患者。

## 12. 安全提醒

1. 已暴露过的私钥只能用于测试网，不能用于主网或真实资产。
2. 不要把私钥写入 `.env.example`、README、代码或提交记录。
3. 不要上传真实医疗数据、身份证号、手机号或真实病历。
4. 当前系统用于毕业设计演示，不可直接作为真实医疗系统上线。
